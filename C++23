#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    string s; string t; cin >> s >> t;
    for (int i = 0; i<s.length(); i++){
      if (s[i]!=t[i]){
        cout << i+1;
        return 0;
      }
    } 
 cout << "YES";
}
